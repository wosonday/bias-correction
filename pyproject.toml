[project]
name = "bias-correction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dask>=2026.7.1",
    "numpy>=2.5.1",
    "scipy>=1.18.0",
    "xarray>=2026.7.0",
]
